# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(ctd_seabirdTaskLib)
ADD_LIBRARY(${CTD_SEABIRD_TASKLIB_NAME} SHARED 
    ${CTD_SEABIRD_TASKLIB_SOURCES})

add_dependencies(${CTD_SEABIRD_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CTD_SEABIRD_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CTD_SEABIRD_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CTD_SEABIRD_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CTD_SEABIRD_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CTD_SEABIRD_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CTD_SEABIRD_TASKLIB_HEADERS}
    DESTINATION include/orocos/ctd_seabird)

